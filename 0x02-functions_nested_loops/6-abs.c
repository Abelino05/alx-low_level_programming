#include "main.h"

/**
 * _abs - Compute the absolute of a number
 * @n: The number to compute
 *
 * Return: n
 */

int _abs(int n)
{
	if (n < 0)
	{
		n = -n;
		return (n);
	}
	else
	{
		return (n);
	}
}
