#include <stdio.h>

/**
 * main - Prints alphabet in lowercase except e and q
 * Return: 0 if success
 */

int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	{
		if (c != 'q' && c != 'e')
		{
			putchar(c);
		}
	}
	putchar('\n');
	return (0);
}
